# IE 计算器 V1.1 原型

Vue 3 + Vite + TypeScript 构建的移动端优先原型。

## 启动

```bash
pnpm install
pnpm dev
```

## 构建

- `pnpm build`：标准静态站点
- `pnpm build:single`：生成可离线打开的单 HTML 文件

当前实现：首页、效率计算页、全局 Header/返回/语言切换、响应式布局及中英高棉语字典骨架。高棉语暂由中文回退补齐。
